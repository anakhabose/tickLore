<style>
  /* Hide scrollbar for Chrome, Safari and Opera */
  .no-scrollbar::-webkit-scrollbar {
    display: none;
  }

  /* Hide scrollbar for IE, Edge and Firefox */
  .no-scrollbar {
    -ms-overflow-style: none;  /* IE and Edge */
    scrollbar-width: none;  /* Firefox */
  }

  /* Existing styles */
  .bg-fixed {
    background-attachment: fixed;
    background-size: cover !important;
    background-position: center !important;
    height: 100vh;
    width: 100%;
  }
  
  /* For mobile devices */
  @media (max-width: 768px) {
    .bg-fixed {
      background-attachment: scroll;
      background-size: cover !important;
      background-position: center !important;
    }
  }

  /* Animation for marquee */
  @keyframes marquee {
    0% { transform: translateX(0); }
    100% { transform: translateX(-100%); }
  }
  
  @keyframes marquee-reverse {
    0% { transform: translateX(-100%); }
    100% { transform: translateX(0); }
  }
  
  .animate-marquee {
    animation: marquee 30s linear infinite;
  }
  
  .animate-marquee2 {
    animation: marquee 30s linear infinite;
    animation-delay: 15s;
  }
  
  .animate-marquee-reverse {
    animation: marquee-reverse 30s linear infinite;
  }
  
  .animate-marquee2-reverse {
    animation: marquee-reverse 30s linear infinite;
    animation-delay: 15s;
  }

  /* Add specific text color styles */
  .text-white {
    color: white !important;
  }

  .text-black {
    color: black !important;
  }
</style>

{{>header}}

<!-- Video Section -->
<section class="container mx-auto px-4 bg-black">
  <div class="relative h-[400px]">
    <video 
      src="/images/homeVideo.mp4"
      class="w-full h-full object-cover"
      autoplay
      loop
      muted
      playsinline
    ></video>
    <!-- Overlay Content -->
    <div class="absolute inset-0 flex flex-col items-center justify-center text-white bg-black bg-opacity-40">
      <h1 class="text-4xl md:text-6xl font-bold mb-4 text-center">
        Timeless Masterpieces
      </h1>
      <p class="text-xl md:text-2xl mb-8 text-center max-w-2xl">
       Elevate Your Style with Iconic Craftsmanship
      </p>
      <a href="/user/shop" class="bg-yellow-500 hover:bg-yellow-600 text-black px-6 py-3 rounded-lg text-base font-semibold transition-all duration-300 transform hover:scale-105">
        Shop Now
      </a>
    </div>
  </div>
</section>

<!-- Categories Section -->
<section class="container mx-auto px-4 py-12">
    <h2 class="text-3xl font-bold mb-8 text-center">Categories</h2>
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-8 justify-items-center max-w-6xl mx-auto">
        {{#each categories}}
        <a href="/user/shop?category={{this.category}}" class="group w-full max-w-[400px]">
            <div class="relative overflow-hidden rounded-lg aspect-square">
                <img 
                    src="{{this.image}}" 
                    alt="{{this.category}}" 
                    class="w-full h-full object-cover transform transition-transform duration-500 group-hover:scale-110"
                >
                <div class="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center transition-opacity duration-300 opacity-0 group-hover:opacity-100">
                    <h3 class="text-white text-2xl font-bold text-center px-4">Shop</h3>
                </div>
              
            </div>
                <!-- Category name below image -->
                <h3 class="text-xl font-bold text-gray-800 mt-4 text-center">{{this.category}}</h3>
        </a>
        {{/each}}
    </div>
</section>

<!-- Latest Releases Section -->
<section class="py-16 bg-gray-100">
    <div class="container mx-auto px-4">
        <h2 class="text-4xl font-bold text-center mb-12">Top Products</h2>
        {{!-- <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-6">
            {{#each latestProducts}}
            <div class="group bg-white rounded-lg overflow-hidden shadow-sm hover:shadow-md transition-all duration-300 h-[400px]">
                <!-- Image Container with Zoom Effect -->
                <div class="relative h-[280px] overflow-hidden">
                    <img 
                        src="{{this.images.[0]}}" 
                        alt="{{this.name}}" 
                        class="w-full h-full object-cover transform transition-transform duration-500 group-hover:scale-110"
                        loading="lazy"
                    >
                    <!-- Hover Overlay with View Button -->
                    <div class="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity duration-300">
                        <a href="/user/viewProducts/{{this._id}}" class="bg-white text-black px-6 py-2 rounded-full text-sm font-medium hover:bg-gray-100 transform hover:scale-105 transition-all">
                            View Details
                        </a>
                    </div>
                </div>
                <!-- Product Info -->
                <div class="p-4">
                    <h3 class="text-lg font-medium text-gray-900 mb-2 truncate">{{this.name}}</h3>
                    <div class="flex justify-between items-center">
                        <p class="text-lg font-bold text-gray-900">₹{{this.price}}</p>
                    </div>
                </div>
            </div>
            {{/each}}
        </div> --}}
    </div>
</section>

<!-- First Text Section -->
<section class="relative min-h-screen w-full flex items-center justify-center bg-white"> 
    <div class="relative z-20 text-center px-4">
        <h2 class="text-4xl md:text-6xl font-bold mb-4 text-black !important leading-tight">
         
"Time flows, but style stays eternal.<br> Wear your legacy on your wrist."
        </h2>
    </div>
</section>

<!-- Third Parallax Section -->
<section class="relative min-h-screen w-full bg-fixed bg-center bg-no-repeat bg-cover flex items-center justify-center" 
    style="background-image: url('../images/scroll3.png'); background-size: cover; background-position: center;">
    <div class="absolute inset-0 bg-white/10"></div>
    <div class="relative z-20 text-center px-4">
        <h2 class="text-4xl md:text-5xl font-bold mb-4 text-black !important">Exclusive Design</h2>
        <p class="text-xl md:text-2xl text-black !important">Where Style Meets Innovation</p>
    </div>
</section>

<!-- Main Content -->
<main class="container mx-auto py-10">
  <div class="max-w-7xl mx-auto p-6">
    <div class="flex justify-between items-center mb-6">
      <h1 class="text-2xl font-bold">latestProducts</h1>
     
    </div>
   
   <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-6">
            {{#each latestProducts}}
            <div class="group bg-white rounded-lg overflow-hidden shadow-sm hover:shadow-md transition-all duration-300 h-[400px]">
                <!-- Image Container with Zoom Effect -->
                <div class="relative h-[280px] overflow-hidden">
                    <img 
                        src="{{this.images.[0]}}" 
                        alt="{{this.name}}" 
                        class="w-full h-full object-cover transform transition-transform duration-500 group-hover:scale-110"
                    >
                    <!-- Hover Overlay with View Button -->
                    <div class="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity duration-300">
                        <a href="/user/viewProducts/{{this._id}}" class="bg-white text-black px-6 py-2 rounded-full text-sm font-medium hover:bg-gray-100 transform hover:scale-105 transition-all">
                            View Details
                        </a>
                    </div>
                </div>
                <!-- Product Info -->
                <div class="p-4">
                    <h3 class="text-lg font-medium text-gray-900 mb-2 truncate">{{this.name}}</h3>
                    <div class="flex justify-between items-center">
                        <p class="text-lg font-bold text-gray-900">₹{{this.price}}</p>
                    </div>
                </div>
            </div>
            {{/each}}
        </div>
        <div class="text-center mt-12">
            <a href="/user/shop" 
               class="inline-block bg-black text-white px-8 py-3 rounded-full text-lg font-medium hover:bg-gray-800 transform hover:scale-105 transition-all duration-300 shadow-md">
                View More Products
            </a>
        </div>
  </div>
</main>


     <!-- Services Section -->
  <div class="max-w-7xl mx-auto px-6 py-12">
    <!-- Card Container -->
    <div class="bg-white rounded-lg shadow-md p-8 grid grid-cols-1 md:grid-cols-3 gap-6">
      
      <!-- Service 1 -->
      <div class="flex flex-col items-center text-center bg-yellow-50 rounded-lg p-6 shadow-sm">
        <div class="bg-black text-white rounded-full p-4 mb-4">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-8 h-8">
            <path stroke-linecap="round" stroke-linejoin="round" d="M3.375 19.5h17.25m-9-16.5v16.5m0 0a3.375 3.375 0 11-6.75 0m13.5 0a3.375 3.375 0 11-6.75 0m6.75 0H6.75" />
          </svg>
        </div>
        <h3 class="font-semibold text-lg text-gray-800">FREE AND FAST DELIVERY</h3>
        <p class="text-sm text-gray-600">Free delivery for all orders over 500 Rupees</p>
      </div>

      <!-- Service 2 -->
      <div class="flex flex-col items-center text-center bg-yellow-50 rounded-lg p-6 shadow-sm">
        <div class="bg-black text-white rounded-full p-4 mb-4">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-8 h-8">
            <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12h15m-7.5 7.5V12m0 0a7.5 7.5 0 100-15 7.5 7.5 0 000 15z" />
          </svg>
        </div>
        <h3 class="font-semibold text-lg text-gray-800">24/7 CUSTOMER SERVICE</h3>
        <p class="text-sm text-gray-600">Friendly 24/7 customer support</p>
      </div>

      <!-- Service 3 -->
      <div class="flex flex-col items-center text-center bg-yellow-50 rounded-lg p-6 shadow-sm">
        <div class="bg-black text-white rounded-full p-4 mb-4">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-8 h-8">
            <path stroke-linecap="round" stroke-linejoin="round" d="M17.25 6.75L6.75 17.25m0 0h9.75a3.75 3.75 0 003.75-3.75V7.5a3.75 3.75 0 00-3.75-3.75H9a3.75 3.75 0 00-3.75 3.75v3.75a3.75 3.75 0 003.75 3.75h3.75" />
          </svg>
        </div>
        <h3 class="font-semibold text-lg text-gray-800">MONEY BACK GUARANTEE</h3>
        <p class="text-sm text-gray-600">Refund within 14 days</p>
      </div>
      
    </div>
  </div>


  <!-- Footer -->
  {{>userFooter}}



  <script>
 
  function redirectToDetailPage(productId) {
    if (productId) {
        window.location.href = `/user/viewProducts/${productId}`;
    }
}
if (window.location.search.includes('error')) {
    const newUrl = window.location.pathname;
    window.history.replaceState({}, document.title, newUrl);
}

 // Add the new back button handling code
    window.addEventListener('pageshow', function(event) {
        if (event.persisted) {
            window.location.reload();
        }
    });

    window.addEventListener('popstate', function(event) {
        window.location.reload();
    });
</script>


