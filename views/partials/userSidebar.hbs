  <aside class="bg-gray-100 text-black w-64 p-4 flex flex-col">
            <div class="text-lg font-bold mb-6">Manage My Account</div>
            <nav class="flex flex-col space-y-4">
                <a href="/user/profile"
                    class="p-2 rounded {{#if (eq currentPath '/user/profile')}}bg-yellow-500 text-white{{else}}text-black hover:bg-yellow-200 hover:text-white{{/if}}">
                    <i class="fas fa-user"></i> My Profile
                </a>
                <a href="/user/orders"
                    class="p-2 rounded {{#if (eq currentPath '/user/orders')}}bg-yellow-500 text-white{{else}}text-black hover:bg-yellow-200 hover:text-white{{/if}}">
                    <i class="fas fa-box"></i> My Orders
                </a>
                <a href="/user/address"
                    class="p-2 rounded {{#if (eq currentPath '/user/address')}}bg-yellow-500 text-white{{else}}text-black hover:bg-yellow-200 hover:text-white{{/if}}">
                    <i class="fas fa-location-arrow"></i> Saved Address
                </a>
                <a href="/user/wallet"
                    class="p-2 rounded {{#if (eq currentPath '/user/wallet')}}bg-yellow-500 text-white{{else}}text-black hover:bg-yellow-200 hover:text-white{{/if}}">
                    <i class="fas fa-wallet"></i> My Wallet
                </a>
                <a href="/user/changePassword"
                    class="p-2 rounded {{#if (eq currentPath '/user/changePassword')}}bg-yellow-500 text-white{{else}}text-black hover:bg-yellow-200 hover:text-white{{/if}}">
                    <i class="fas fa-key"></i> Change Password
                </a>
                <a href="/user/logout"
                    class="p-2 rounded {{#if (eq currentPath '/user/logout')}}bg-yellow-500 text-white{{else}}text-black hover:bg-yellow-200 hover:text-white{{/if}}">
                    <i class="fas fa-sign-out-alt"></i> Logout
                </a>
            </nav>
        </aside>